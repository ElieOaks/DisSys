#Securty module

class security:

    message = "some string"

    def encrypt_message(message, key_one, key_two):
        return 0
    
    def decrypt_message(cipher_text, key_One, key_two):
        return 0
